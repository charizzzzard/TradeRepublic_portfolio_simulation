[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    phase_artifact: asserts on a phase run's own output artifacts. Excluded from that
        phase's own acceptance check, because a phase cannot validate itself by a test
        that reads its own result.
