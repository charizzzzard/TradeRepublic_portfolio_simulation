# Transitive pins for `pip install -r requirements.txt -c constraints.txt`.
# Only pytest's runtime dependencies appear here; none affect numerics.
iniconfig==2.3.0
packaging==24.0
pluggy==1.6.0
pygments==2.21.0
